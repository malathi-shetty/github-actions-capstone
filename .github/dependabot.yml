# .github/dependabot.yml

version: 2

updates:
  - package-ecosystem: "github-actions"

    directory: "/"

    schedule:
      interval: "weekly"

    commit-message:
      prefix: "deps"

    labels:
      - "security"
      - "dependencies"

    open-pull-requests-limit: 5
